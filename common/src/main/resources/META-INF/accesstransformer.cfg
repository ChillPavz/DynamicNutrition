# Same two widenings as the Fabric access widener, in NeoForge spelling. Class names are
# official on 26.x, and so are member names, because the game ships de-obfuscated.
# Blocks are separated with blank lines rather than lone hashes: a comment line with no text
# aborts the whole transform, and reports itself as a missing artifact rather than as a parse error.

public net.minecraft.client.gui.screens.inventory.AbstractContainerScreen leftPos

public net.minecraft.client.gui.screens.inventory.AbstractContainerScreen topPos
